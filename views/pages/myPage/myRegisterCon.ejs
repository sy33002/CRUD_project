<!DOCTYPE html>
<html lang="ko">
    <head>
        <%- include('../../common/head.ejs', {title: '내가 등록 신청한 행사' })
        %>

        <link
            rel="stylesheet"
            href="/static/css/pages/myPage/managerAside.css"
        />
        <style>
            .container {
                padding: 20px 20px;
            }
        </style>
    </head>

    <body>
        <%- include('../../common/header.ejs') %>

        <main>
            <%- include('./profileAside', { id: id }) %>
            <div class="container content">
                <table class="table text-center">
                    <thead>
                        <tr class="border-bottom">
                            <th></th>
                            <th>행사 이름</th>
                            <th>주최사</th>
                            <th>신청한 시간</th>
                            <th>관리 승인 여부</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% data.forEach(function(conference, index) { %>
                        <tr class="border-bottom">
                            <td><%= index + 1 %></td>
                            <td><%= conference.dataValues.con_title %></td>
                            <td><%= conference.dataValues.con_company %></td>
                            <td><%= conference.dataValues.createdAt %></td>
                            <td>
                                <% if (conference.dataValues.is_agreed === 1) {
                                %> 승인 완료 <% } else if
                                (conference.dataValues.is_agreed === 0) { %>
                                승인 대기중 <% } else if
                                (conference.dataValues.is_agreed === -1) { %>
                                거절 <% } else { %> 알 수 없음 <% } %>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </main>
        <%- include('../../common/footer.ejs') %>
        <script></script>
    </body>
</html>
